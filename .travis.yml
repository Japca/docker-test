sudo: required
services:
  - docker

before_install:
  - docker build -t japca/docker-test -f Dockerfile.dev .

scripr:
  - 'gem install bundler'
  - 'rake spec'
  - docker run japca/docker-test npm run test -- --coverage

